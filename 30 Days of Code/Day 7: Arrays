// https://www.hackerrank.com/challenges/30-arrays
// C++

#include <bits/stdc++.h>

using namespace std;

vector<string> split_string(string);

void printElementsInReverseOrder(vector<int> arr) {
   int i = 0;

   for (i = arr.size() - 1; i >= 0; i--) {
       cout<<arr.at(i)<<" ";
   }
}
